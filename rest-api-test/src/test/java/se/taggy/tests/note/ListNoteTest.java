package se.taggy.tests.note;

import org.junit.Assert;
import se.taggy.tests.util.TestBase;

public class ListNoteTest {

    public static Note loadNote(TestBase base, int noteId) {
        /*base.get("/notes/" + noteId, (response, responseAsJson) -> {

        });*/
        Assert.fail("Not implemented yet");
        return null;
    }
}
